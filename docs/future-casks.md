# Future Casks

`Casks/` exists as an empty placeholder only. V1 does not publish casks because no notarized macOS apps are part of this tap yet.

When a cask is added, require notarization notes, an immutable download URL, a verified checksum, and uninstall/zap behavior.
